# Animal Band – BPM Sync (Vite + React + PixiJS)

## Local run
1) Install Node.js (LTS)
2) In terminal:
```bash
npm install
npm run dev
```

## Deploy (Vercel)
- Push this folder to GitHub, then import the repo in vercel.com
